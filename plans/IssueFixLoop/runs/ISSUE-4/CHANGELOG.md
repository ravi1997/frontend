# Changelog

## Added

- Enhanced PWA Metadata in `src/app/layout.tsx`
  - Added `appleWebApp` configuration (capable, statusBarStyle, title).
  - Added `formatDetection` (telephone: false).
  - Added `viewport` export with `themeColor`.
- `next.config.ts` was already correctly configured for PWA.
- `manifest.json` was already present.

## Fixed

- N/A
