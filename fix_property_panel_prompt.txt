Fix Property Panel Close Button Implementation
File: /home/ravi/workspace/frontend/lib/modules/dashboard_builder/pages/dashboard_builder_page.dart

Issue:
The `_PropertyPanel` (Properties panel) lacks a visible close button. Clicking "close" does nothing because:
1. The panel only has a delete button (`Icons.delete_outline_rounded`)
2. No `onPressed` handler exists for closing the panel
3. Parent (`DashboardBuilderPage`) expects `onClose` callback but it's not implemented

Required Fix:
1. Add close button to title bar (before delete button) with:
   IconButton(
     icon: const Icon(Icons.close, size: 18),
     color: cs.onSurfaceVariant,
     onPressed: widget.onClose,
     tooltip: 'Close properties',
     padding: EdgeInsets.zero,
     constraints: const BoxConstraints(),
   )
2. Ensure parent implements `onClose`:
   void _closePropertyPanel() => setState(() => _selectedWidget = null);
   Then pass to panel:
   _PropertyPanel(
     onClose: _closePropertyPanel,
     // ...other params
   )

Do NOT:
- Modify existing delete button logic
- Change panel layout structure
- Add new state management (use existing `_selectedWidget` state)

Verification:
After fix, clicking close button should:
1. Trigger `onClose` callback
2. Set `_selectedWidget = null` in parent
3. Dismiss panel without deleting widget