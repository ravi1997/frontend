# Stack Gate: Node.js

## Rules

1. **Linting**: Must pass `eslint`.
2. **Security**: `npm audit` must return 0 high vulnerabilities.
3. **Structure**: Follow the configured directory structure (e.g., `src/`, `dist/`).

## Remediation

- Run `npm run lint -- --fix`.
