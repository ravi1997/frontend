# Stack Gate: Flutter

## Rules

- **Lint**: Must pass `flutter analyze` with no errors.
- **Formatting**: Must pass `dart format`.
- **Naming**: Use `CamelCase` for classes and `snake_case` for files.
- **Widgets**: Keep widgets small and specialized.
