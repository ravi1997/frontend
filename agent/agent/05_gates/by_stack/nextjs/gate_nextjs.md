# Stack Gate: Next.js

## Rules

- **Server Components**: Use Server Components by default.
- **Routing**: Follow the `app/` directory convention.
- **Metadata**: Every page must have a title and description for SEO.
- **Data Fetching**: Use `fetch()` with appropriate caching tags.
