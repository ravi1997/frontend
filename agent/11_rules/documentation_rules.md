# Documentation Rules

## 1. Real-time Accuracy

- Documentation must never lag behind the code. If code changes, update the corresponding `plans/` file.

## 2. Clarity

- Use diagrams (Mermaid) for complex flows.
- Include "Example Usage" for public APIs.

## 3. Structure

- Follow the templates in `07_templates/`.
