# Stack Rule: Python

## Rules

- **PEP 8**: Follow the official style guide.
- **Dependencies**: Always update `requirements.txt` or `pyproject.toml`.
- **Virtual Environments**: Always check for or create a `.venv`.
- **Type Hints**: Use `typing` module for all function arguments and return types.
