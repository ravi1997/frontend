# Stack Fingerprint: C Make

## Purpose

Identifies and configures for a C project using Makefile.

## Signals

- `Makefile` in root.
- `.c`, `.h` files.
- Lack of C++ signals.

## Configuration

- **Build**: `make`.
- **Clean**: `make clean`.
- **Compiler**: gcc or clang.
