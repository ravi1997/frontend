# Skill: Dockerize Service

## Purpose

Creating a container for a project.

## Procedure

1. Identify the runtime and dependencies.
2. Generate a `Dockerfile` using the project's preferred base image.
3. Generate `docker-compose.yml` if external services (DB, Redis) are needed.
4. Test with `docker build .`.
